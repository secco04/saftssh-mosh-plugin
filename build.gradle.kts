// Top-level build file — plugin declarations only.
// Note: org.jetbrains.kotlin.android is intentionally absent — AGP 9.0+ includes
// built-in Kotlin support and conflicts with the standalone kotlin.android plugin.
plugins {
    id("com.android.application") version "9.1.0" apply false
}
